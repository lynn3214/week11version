# Data and output directory paths

data:
  raw_dir: data/raw
  training_sources: data/raw/training_sources  # 新增
  test_raw: data/test_raw                      # 新增
  training_resampled: data/training_resampled
  test_resampled: data/test_resampled
  noise_resampled: data/noise_resampled
  augmented_clicks: data/augmented_clicks      # 新增
  noise_segments: data/noise_segments          # 新增
  training_segs: data/training_segs            # 新增

preprocessing:
  scan_manifest: data/preprocessing/scan_manifest
  convert_and_filter: data/preprocessing/convert_and_filter

detection:
  rules: data/detection/rules
  candidate_finder: data/detection/candidate_finder
  segmenter: data/detection/segmenter
  features_event: data/detection/features_event
  train_builder: data/detection/train_builder
  fusion: data/detection/fusion
  export: data/detection/export

training:
  dataset: data/training/dataset
  augment: data/training/augment
  checkpoints: models/checkpoints
  logs: logs/training

reports:
  qa_plots: reports/qa_plots
  metrics: reports/metrics
  output: reports/output